import React, { useCallback, useState } from 'react';
import { Link } from 'react-router-dom';
import { bloosLogoFull } from '../tools/global';

const Home = () => {
  return <div className='text-slate-800 isolate bg-white min-h-screen'></div>;
};

export default Home;
